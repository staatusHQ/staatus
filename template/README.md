# Staatus Template

This directory sketches the future forkable status-page repository shape.

For v0, the root repository already contains the working sample config, canonical data files, renderer output, and web app. A dedicated template repo can be split out once the CLI and public API contracts settle.
